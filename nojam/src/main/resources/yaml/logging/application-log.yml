spring:
  config:
    activate:
      on-profile: log
  jpa:
    properties:
      hibernate:
        format_sql: true
        highlight_sql: true
        use_sql_comments: true
logging:
  level:
    org:
      springframework:
        orm:
          jpa: DEBUG